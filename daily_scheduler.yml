name: Daily AlphaQuant Automation

on:
  schedule:
    # 6:25 UTC = 11:55 AM IST (Testing NOW)
    - cron: '25 6 * * *'
  workflow_dispatch: # Keeps the manual button so you can test whenever

permissions:
  contents: write

jobs:
  run-analytics:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install Dependencies
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run AI Engine (Scan & Rank)
        run: python main.py

      - name: Send Newsletter (With Dashboard Link)
        env:
          EMAIL_PASSWORD: ${{ secrets.APP_PASSWORD }} 
        run: python email_bot.py

      - name: Commit & Push New Data
        uses: stefanzweifel/git-auto-commit-action@v4
        with:
          commit_message: "Daily Data Update"
          file_pattern: "*.csv"

